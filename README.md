# API for University

Pet-проект для изучения FastAPI. Проект представляет собой REST API для управления университетской системой с поддержкой пользователей, групп и информации о студентах.

## 📋 Описание

Этот проект создан на основе бэкенда из хакатона от **MAX** и **VK Education** ([исходный проект](https://github.com/dencamenew/VSUET.git)), где я выступал в роли backend разработчика на FastAPI. Функционал бэкенда с хакатона переносится в этот проект с доработками и улучшениями.

### Основной функционал:

- 🔐 **Аутентификация и авторизация** через JWT токены
- 👥 **Управление пользователями** с ролевой моделью (admin, teacher, student)
- 📚 **Управление группами** с расписанием в формате JSON
- 🎓 **Управление информацией о студентах** (личные данные, зачетная книжка, статус)

## 🛠 Технологический стек

- **FastAPI** - современный веб-фреймворк для создания API
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **Pydantic** - валидация данных
- **JWT** - аутентификация
- **Docker & Docker Compose** - контейнеризация и оркестрация

## 📁 Структура проекта

```
.
├── backend/
│   └── app/
│       ├── core/           # Основные настройки (JWT, БД, пароли)
│       ├── models/         # ORM и Pydantic модели
│       ├── repositories/   # Репозитории для работы с БД
│       ├── routers/        # API эндпоинты
│       ├── services/       # Бизнес-логика
│       └── main.py         # Точка входа приложения
├── db/
│   └── postgres/
│       └── init/           # SQL скрипты инициализации БД
├── docker-compose.yaml     # Конфигурация Docker Compose
├── Dockerfile              # Образ для FastAPI приложения
└── requirements.txt        # Python зависимости
```

## 🚀 Быстрый старт

### Требования

- Docker >= 20.x
- Docker Compose >= 2.x

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd API-for-University
   ```

2. **Создайте файл `.env` в корне проекта:**
   ```env
   POSTGRES_USER=your_user
   POSTGRES_PASSWORD=your_password
   POSTGRES_HOST=postgres
   POSTGRES_PORT=5432
   POSTGRES_DB=your_database
   POSTGRES_SCHEMA=public
   SECRET_KEY=your_secret_key
   ALGORITHM=HS256
   API_PORT=8000
   ```

3. **Запустите проект через Docker Compose:**
   ```bash
   docker-compose up -d --build
   ```

4. **Проверьте статус сервисов:**
   ```bash
   docker-compose ps
   ```

5. **API будет доступно по адресу:**
   - API: http://localhost:8000
   - Документация: http://localhost:8000/docs
   - Альтернативная документация: http://localhost:8000/redoc

### Остановка проекта

```bash
docker-compose down
```

Для полной очистки (включая volumes):

```bash
docker-compose down --volumes --remove-orphans
```

## 📚 API Эндпоинты

### Аутентификация

- `POST /auth/log-in` - Получение JWT токена

### Пользователи

- `POST /user/add` - Создание пользователя (admin only)

### Группы

- `POST /group/add` - Создание группы (admin, teacher)
- `GET /group/` - Получение всех групп
- `GET /group/{id}` - Получение группы по ID
- `PUT /group/{id}` - Обновление группы (admin, teacher)
- `DELETE /group/{id}` - Удаление группы (admin only)

### Информация о студентах

- `POST /student-info/add` - Создание записи о студенте (admin, teacher)
- `GET /student-info/` - Получение всех студентов (admin, teacher)
- `GET /student-info/{id}` - Получение студента по ID
- `GET /student-info/user/{user_id}` - Получение студента по user_id
- `GET /student-info/group/{group_id}` - Получение студентов группы
- `PUT /student-info/{id}` - Обновление информации (admin, teacher)
- `DELETE /student-info/{id}` - Удаление информации (admin only)

## 🔒 Роли и права доступа

- **Admin** - полный доступ ко всем операциям
- **Teacher** - создание и обновление групп и студентов, чтение всех данных
- **Student** - только чтение данных

## 🧪 Тестовые данные

После инициализации БД создаются тестовые пользователи:

- **Admin**: `admin` / `AdminPass1!`
- **Student**: `student1` / `StudentPass1!`
- **Teacher**: `teacher1` / `TeacherPass1!`

## 📝 Разработка

### Локальная разработка без Docker

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Убедитесь, что PostgreSQL запущен и доступен

3. Запустите приложение:
   ```bash
   cd backend
   python -m uvicorn app.main:app --reload
   ```

### Структура кода

- **Repositories** - слой доступа к данным (CRUD операции)
- **Routers** - HTTP эндпоинты с валидацией и контролем доступа
- **Services** - бизнес-логика приложения
- **Models** - ORM модели и Pydantic схемы